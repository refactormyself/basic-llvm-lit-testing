# NOTICE: I am not including the test_format which is required, 
#         lit will use the one declared in the parent dir. 

# can't override the name, apparently
# config.name = "Here is the C language version of the test"
config.suffixes = ['.cpp', '.py']

if config.do_whatever_i_want != "i am humble":
    # this marks this dir (all tests inside it) as unsupported.
    config.unsupported = True

